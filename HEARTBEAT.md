# HEARTBEAT.md - Periodic Tasks

```markdown
# Keep this file empty (or with only comments) to skip heartbeat API calls.

# Add tasks below when you want periodic background checks.
```

---

_Use cron for precise scheduling. Use heartbeat for loose periodic checks (every ~30 min is fine)._
