"use client";
import { useEffect } from 'react'
import { useRouter } from 'next/navigation'

export default function ITPortalRoot() {
  // Simple client-side redirect to internal dashboard
  // This keeps the root path clean and ensures internal flow starts at the dashboard
  // The actual navigation can be controlled by the app's internal routing if needed
  return null
}
